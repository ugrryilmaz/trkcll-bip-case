import ProductList from '@/features/products/components/ProductList';
import Layout from '@/layout/Layout';

const App = () => {
  return (
    <Layout>
      <ProductList />
    </Layout>
  );
};

export default App;
